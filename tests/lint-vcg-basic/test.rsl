package Test

type T1 {
    x optional Integer
    y optional Integer
    z          Integer
}

checks T1 {
  x != null implies y == x, fatal "potato"
  y != null implies x > 1, warning "potato"
}

type T2 {
  x Integer
}

checks T2 {
  x > 1, fatal "must be positive", x
  x != 0, warning "potato"
}
