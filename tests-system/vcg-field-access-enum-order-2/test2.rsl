package Test2

enum Color {
  Red
  Green
}

type Item {
  colors Color [1 .. *]  // having this component is essential for the test - do not remove!
  next   optional Item
}

type Container {
  items optional Item [1 .. *]
}

checks Container {
  items != null implies
    (forall item in items => item.next == null),
    warning "an item in the container has a successor"
}
