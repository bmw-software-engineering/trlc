          package bar


            type MyType {
               name optional String
            }
             checks MyType {
                name != null and name == "Qbert", error "name must be 'Qbert'", name
                   }
