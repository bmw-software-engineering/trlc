c foo.bar.qux.E
          ^^^ nested-packages-wildcard-self-descendant/foo_bar.rsl:8: error: cannot refer to sub-package foo.bar.qux of the current package
            | package foo.bar is elaborated before its sub-packages, so foo.bar.qux cannot be used here; move the declarations you need into foo.bar itself, or into a package outside the foo.bar subtree
Processed 3 models and 0 requirement files and found 1 error
