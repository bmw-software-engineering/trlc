          package bar



            type MyType {
                name String
                length Integer
            }
             checks MyType {
              name != null and len(name) == length, "name must have specific length" , name
               }

            


            type Type2 extends MyType {

            }