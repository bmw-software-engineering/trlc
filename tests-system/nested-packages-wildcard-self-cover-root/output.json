c foo.bar.Color
      ^^^ nested-packages-wildcard-self-cover-root/foo.rsl:10: error: cannot refer to sub-package foo.bar of the current package
        | package foo is elaborated before its sub-packages, so foo.bar cannot be used here; move the declarations you need into foo itself, or into a package outside the foo subtree
Processed 2 models and 0 requirement files and found 1 error
